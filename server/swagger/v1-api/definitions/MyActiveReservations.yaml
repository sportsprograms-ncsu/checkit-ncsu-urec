items:
  properties:
    asset:
      properties:
        id: {description: the assetID associated with this reservation, type: string}
        name: {description: the title of the asset reserved, type: string}
      type: object
    end: {description: the ending date of this reservation in ISO format, type: string}
    id: {description: ObjectId generated by mongoose, type: string}
    start: {description: the starting date of this reservation in ISO format, type: string}
    status:
      description: the status of this reservation
      enum: [checked_out, reserved]
      type: string
  type: object
type: array
